# CarbonWise AI Environment Variables Example

# Backend configuration (Server)
PORT=5000

# Frontend configuration (Client)
# Replace with your deployed Render backend URL (no trailing slash)
VITE_API_URL=https://YOUR-BACKEND.onrender.com
